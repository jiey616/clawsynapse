.DS_Store
bin/
dist/
*.log
.env
/clawsynapse
.gocache/

# ── Local build artifacts (not tracked) ──
.go/
*.exe
.hermes_install_tmp.sh

# ── WorkBuddy workspace (dev only) ──
.workbuddy/

# ── Local-only Dockerfile (tencent mirror, for local docker build only) ──
Dockerfile.local

# ── Benchmark test artifacts (not part of the release) ──
bench_*
# ── Local-only temporary node compose (not part of the repo) ──
docker-compose.tmp.yml

# ── Local build artifacts / backups (not part of the repo) ──
bin-linux/
roundtable/director-profile-backup-*/

# ── Local-only roundtable add-on (kept out of the repo) ──
roundtable/
Dockerfile.roundtable
Dockerfile.rt-patch
docker-compose.roundtable.yml

# ── Local git recovery backups ──
.git-broken*/

# ── Local build artifacts & scratch (Phase 3.6 cleanup) ──
_build/
_scratch_probe/
bin-fresh/
*.new
config.example.yaml.orig
docker-entrypoint.sh.*-bak
docker-entrypoint.nohermes.sh
Dockerfile.nohermes
Dockerfile.claw5-fresh
Dockerfile.rt2-soul
Dockerfile.rt3-fix
Dockerfile.rt4-final
awesome-dsh-plugin-main*
docker-compose.claw*.yml
docker-compose.second.yml
docker-compose.third.yml
